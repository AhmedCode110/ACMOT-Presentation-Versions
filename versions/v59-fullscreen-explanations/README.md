# AC-MOT — full-screen explanations (v59)

This version keeps the detailed explanation buttons and makes the explanation view fill the
browser window. The author name on the final slide now uses the same `Capt. Eng.` form as the
title slide.

This version is copied from v58. Older versions were not changed.
