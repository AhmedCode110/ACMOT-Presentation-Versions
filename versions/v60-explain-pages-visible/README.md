# AC-MOT — visible explanation pages (v60)

This version keeps the full-screen explanation view and large text, and explicitly controls
which explanation page is visible. This prevents the inner detail view from appearing blank.
The final slide keeps the author name as `Capt. Eng. Ahmed Gouda Ismail`.

This version is copied from v59. Older versions were not changed.
