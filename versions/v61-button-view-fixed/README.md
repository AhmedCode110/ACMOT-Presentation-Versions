# AC-MOT — fixed button detail view (v61)

This version keeps the large full-screen explanation slides and forces the active explanation
layer above the main presentation. Opening an explanation button now directly shows the first
real detail page and hides the other pages.

This version is copied from v60. Older versions were not changed.
